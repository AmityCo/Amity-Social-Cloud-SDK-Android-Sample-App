<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.ekoapp.sample.socialfeature">

    <dist:module
        dist:instant="false"
        dist:onDemand="true"
        dist:title="@string/title_social_feature">
        <dist:fusing dist:include="true" />
    </dist:module>

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher">
        <activity android:name=".createfeeds.CreateFeedsActivity" />
        <activity android:name=".editfeeds.EditFeedsActivity" />
        <activity android:name=".userfeeds.view.UserFeedsActivity" />
        <activity android:name=".search.SearchUsersActivity" />
        <activity android:name=".users.view.SeeAllUsersActivity" />
        <activity android:name=".reactions.view.ReactionsSummaryFeedsActivity" />
        <activity android:name=".userfeeds.view.BodyUserFeedsActivity" />
    </application>

</manifest>

