<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.ekoapp.sample.chatfeature">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <dist:module
        dist:instant="false"
        dist:onDemand="true"
        dist:title="@string/title_chat_feature">
        <dist:delivery>
            <dist:on-demand />
        </dist:delivery>
        <dist:fusing dist:include="true" />
    </dist:module>

    <application>
        <activity android:name=".settings.ChannelSettingsActivity" />
        <activity android:name=".messages.MessagesActivity" />

        <activity android:name=".messagelist.ParentMessageListActivity" />
        <activity android:name=".messagelist.ChildMessageListActivity" />
        <activity android:name=".messagelist.ImageMessageSenderActivity" />
        <activity android:name=".messagelist.FileMessageSenderActivity" />
        <activity android:name=".messagelist.CustomMessageSenderActivity" />
        <activity android:name=".messagereactionlist.MessageReactionListActivity" />
        <activity android:name=".usermetadata.ChangeMetadataActivity" />

        <service
            android:name="com.ekoapp.sample.chatfeature.services.SimpleFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>

